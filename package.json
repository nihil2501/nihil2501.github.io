{
  "name": "elm-pages-app",
  "scripts": {
    "postinstall": "elm-tooling install",
    "start": "elm-pages dev",
    "build": "elm-pages build && rm -rf _site && mv dist _site",
    "add": "elm-pages add"
  },
  "devDependencies": {
    "@notionhq/client": "^2.2.3",
    "dotenv": "^16.0.3",
    "elm-optimize-level-2": "0.2.3",
    "elm-pages": "2.1.10",
    "elm-review": "^2.5.5",
    "elm-tooling": "^1.4.0",
    "notion-exporter-with-images": "^0.4.1",
    "slug": "^8.2.2"
  }
}
